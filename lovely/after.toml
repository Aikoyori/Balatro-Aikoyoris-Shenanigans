[manifest]
version = "1.2"
dump_lua = true
priority = 1073741823

[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = """            if add and not G.GAME.banned_keys[v.key] then """
position = "before"
payload = """-- print("they key is "..v.key.. " and " .. (add and "added" or "FUCK YOU"))
"""
overwrite = true
match_indent = true
[[patches]]
[patches.pattern]
target = "functions/common_events.lua"
pattern = """            if add and not G.GAME.banned_keys[v.key] then """
position = "after"
payload = """--   print(v.key .. " is added congratualtuions")
"""
overwrite = true
match_indent = true


# redux arcanum uses the same patch area
# it got me oooh i wanna

[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = """        if modded then update_hand_text({sound = 'chips2', modded = modded}, {chips = hand_chips, mult = mult}) end"""
position = "before"
payload = """if not akyrs_no_default_update then"""
overwrite = true
match_indent = true


[[patches]]
[patches.pattern]
target = "functions/state_events.lua"
pattern = """        if modded then update_hand_text({sound = 'chips2', modded = modded}, {chips = hand_chips, mult = mult}) end"""
position = "after"
payload = """end"""
overwrite = true
match_indent = true


[[patches]]
[patches.copy]
target = "main.lua"
position = "append"
sources = [
    "modules/hooks/late_hooks.lua",
]


[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = """      if AUT.badges.card_type or AUT.badges.force_rarity then"""
position = "before"
payload = """
local multi_boxes = {}
local info_cols = {}
if obj and AKYRS.should_conceal_card(card, obj) then

else"""
overwrite = true
match_indent = true



[[patches]]
[patches.pattern]
target = "functions/UI_definitions.lua"
pattern = """          table.insert(info_cols, {n=G.UIT.C, config = {align="cm"}, nodes = col})
      end"""
position = "after"
payload = """end"""
overwrite = true
match_indent = true
